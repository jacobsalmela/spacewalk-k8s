apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: spacewalk-postgres-claim
  labels:
    app: spacewalk
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
