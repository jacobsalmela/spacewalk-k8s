apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: spacewalk-httpd-conf-claim
  labels:
    app: spacewalk
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Mi
